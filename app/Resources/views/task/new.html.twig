{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <h1>New Task</h1>
                {{ form_start(taskForm) }}
                {{ form_widget(taskForm) }}
                <button type="submit" class="btn btn-primary">Save</button>
                {{ form_end(taskForm) }}
            </div>
        </div>
    </div>
{% endblock %}