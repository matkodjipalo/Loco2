default:
    suites:
        default:
            contexts:
                - FeatureContext
                - Behat\MinkExtension\Context\MinkContext
            paths: [ %paths.base%/features/web ]
        commands:
            contexts:
                - CommandLineProcessContext
            paths: [ %paths.base%/features/commands ]
    extensions:
        Behat\MinkExtension:
            base_url: http://localhost/web/app_dev.php/
            goutte: ~
            selenium2: ~
            show_cmd: 'firefox %s'
        Behat\Symfony2Extension: ~